name: cliniprax_osce
description: A medical clinic assistant and OSCE simulator application.
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.5
  google_fonts: ^6.1.0
  flutter_vector_icons: ^2.0.0
  supabase_flutter: ^2.6.0
  shared_preferences: ^2.2.0
  device_info_plus: ^10.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1

flutter:
  uses-material-design: true
